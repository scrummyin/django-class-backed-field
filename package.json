{
    "name": "django-class-backed-field",
    "description": "This project has a field which extends a charfield and will pass the string stored in the database to the constructor of the object specified",
    "install_requires": [
        "Django"
    ]
}
